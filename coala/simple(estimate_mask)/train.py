import torch
import torch.nn as nn
import torch.optim as optim

# TODO: simple training loop for simple cnn model

# needs to fill in masked regions of image with a variety of fills, to help model learn to detect various occluders / masks